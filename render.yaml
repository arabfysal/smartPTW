services:
  - type: web
    name: smart-ptw
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run serve
    envVars:
      - key: NODE_ENV
        value: production
